# Changelog

## [1.1.1] - 2026-03-22
### Fixed
- **XAML Parse Error**: Fixed a critical crash where `Style` for `RadioButton` was incorrectly applied to standard `Button` elements. Introduced `SideBtn` style to resolve this.
- **Code Stability**: Fixed two lint warnings: removed unused `statusLabel` variable and replaced `foreach` alias with `ForEach-Object` for better maintenance.

## [1.1.0] - 2026-03-22
...
